version: "2"
services:
  django:
    container_name: ServiseVersion
    build:
      context: .
      dockerfile: Dockerfile
    image: djano_sv_docker
    stdin_open: true
    tty: true
    volumes:
      - .:/var/www/myproject
    ports:
      - "8000:8000"